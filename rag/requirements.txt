# Ingest (M2)
pymupdf>=1.24

# Embedding + server (SPARK)
# klient musí verzí ladit s Chroma serverem na JODA (chromadb/chroma:latest, 1.x)
# — 0.6.x klient padá na KeyError '_type' proti 1.x serveru
chromadb>=1.4,<2
sentence-transformers>=3.0
fastapi>=0.115
uvicorn>=0.30
openai>=1.40
pydantic>=2.0

# Katalog + fulltext (Postgres na JODA), TEI ingest, registry
psycopg[binary]>=3.2
psycopg_pool>=3.2
lxml>=5
pyyaml>=6
pytest>=8
